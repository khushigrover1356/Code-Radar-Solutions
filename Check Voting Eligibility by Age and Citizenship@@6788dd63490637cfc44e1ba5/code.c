#include <stdio.h>

int main() {
    int age,citizenStatus;
    scanf("%d%d",&age,&citizenStatus);
    if (age>=18 && citizenStatus==1){
        printf("Eligible");

    }
    else if {
        printf("Not ELigible");
    }
    return 0;
}