#include <stdio.h>

int main() {
    int age,citizentatus;
    scanf("%d%d",&age,&citizentatus);
    if (age>=18 && citizentatus==1){
        printf("Eligible");

    }
    else if {
        printf("Not ELigible");
    }
    return 0;
}